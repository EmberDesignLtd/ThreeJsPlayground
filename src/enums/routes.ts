/**
 * This will only work as long as we maintain the same level of nesting for the typescript files
 * but as this is a little workshop project should be fine.
 */
export enum Route {
  HOMEPAGE = '../pages/homepage/homepage.html',
  BASIC_SCENE = '../pages/basic_scene/basic_scene.html',
}
